/*
** EPITECH PROJECT, 2020
** OOP_arcade_2019
** File description:
** point
*/

#ifndef POINT_HPP_
#define POINT_HPP_
/*
template<typename T>
class Point {
    public:
        Point(): x(0), y(0);
        Point(T px, T py): x(<static_cast<T>(px)), y(static_cast<T>(py));
        ~Point();

        T getDistance(Point const& a, Point const& b) {

        };

        int x;
        int y;
};*/

struct Point {
    double x;
    double y;
};

#endif /* !POINT_HPP_ */
